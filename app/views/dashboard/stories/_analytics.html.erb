<table class="table table-hover col-md-12">
    <thead class="analytics_table titles">
        <tr>
            <!-- TODO: Insert list of metrics here -->
            <th>Story ID</th>
            <th>Metric 1</th>
            <th>Metric 2</th>
        </tr>
    </thead>
    <tbody class="analytics_table story">
        <% project.stories.each do |story| %>
            <tr>
                <td><%= story.id %></td>
                <td><%= story.current_state %></td>
                <td><%= story.name %></td>
            </tr>
        <% end %>
    </tbody>
</table>